import { Form } from '@/components'
import { WrapperCreateItem } from './createItem.styles'

export const CreateItem = () => {
  return (
    <WrapperCreateItem>
      <Form variant='create' />
    </WrapperCreateItem>
  )
}
